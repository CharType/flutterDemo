

import 'package:flutter/material.dart';

class CQMallContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("市集首页",style: TextStyle(fontSize: 20,color: Colors.green),),
    );
  }
}
