import 'package:flutter/material.dart';

class CQProfileContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("我的首页",style: TextStyle(fontSize: 20,color: Colors.green)),
    );
  }
}
