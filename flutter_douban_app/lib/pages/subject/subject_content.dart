import 'package:flutter/material.dart';

class CQSubjectContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("书影音首页",style: TextStyle(fontSize: 20,color: Colors.green),),
    );
  }
}
